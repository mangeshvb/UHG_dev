<apex:component controller="ACETStateController">   
    <!-- do not name as state, have strange issue under IE when field is disabled-->       
    <apex:selectList id="searchstate" size="1" value="{!State}">
      <apex:selectOptions value="{!StateOptions}"></apex:selectOptions>
    </apex:selectList>          
</apex:component>